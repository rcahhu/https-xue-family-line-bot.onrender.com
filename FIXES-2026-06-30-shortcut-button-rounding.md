# 2026-06-30 shortcut, button color, and settlement rounding

- Changed the single-trip shortcut flow so it is clearly for a home-screen shortcut.
- Removed the automatic external-browser jump from the shortcut button. If direct install is unavailable, the shortcut URL is copied and instructions are shown.
- Unified the trip tab button colors so the shortcut and settlement actions do not look like unrelated buttons.
- Changed pooled equal settlement rounding so the shared average amount is shown consistently for all members instead of distributing one-dollar differences across members.
- Adds a settlement note when a small rounding tail is ignored to keep member totals visually consistent.
